import { createRoot } from "react-dom/client";
import App from "./App";
import { ErrorBoundary, PluginRuntime, PlayerSwitcher, HistoryNavigator } from "@dreamboard/sdk/components";

createRoot(document.getElementById("root")!).render(
  <ErrorBoundary>
    <PluginRuntime>
      <PlayerSwitcher position="top-right" />
      <HistoryNavigator position="bottom-right" />
      <App />
    </PluginRuntime>
  </ErrorBoundary>,
);
