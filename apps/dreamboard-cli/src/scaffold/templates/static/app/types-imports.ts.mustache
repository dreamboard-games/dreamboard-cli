// Imported dynamic types generated from manifest
import type {
  ActionName,
  ActionParametersFor,
  NextStateFor,
  PlayerId,
  StateName,
  CardId,
  DeckId,
} from '../../shared/manifest.ts';

// Re-export for convenience
export type {
  ActionName,
  ActionParametersFor,
  NextStateFor,
  PlayerId,
  StateName,
  CardId,
  DeckId,
};
